# Nothing generated lives in the repo: instance state, credentials, and the
# devcontainer.env all live under ~/.local/state/dcx/. This file exists to keep
# stray local artifacts out.
*.log
.DS_Store
